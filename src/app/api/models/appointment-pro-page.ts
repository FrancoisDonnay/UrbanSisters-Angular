/* tslint:disable */
import { AppointmentPro } from './appointment-pro';
export interface AppointmentProPage {
  items?: Array<AppointmentPro>;
  pageSize?: number;
  pageIndex?: number;
  totalCount?: number;
}
